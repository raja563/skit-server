import React from 'react'

const Result = () => {
  return (
    <div>
      Result
    </div>
  )
}

export default Result

/**
 * model
 * take basic information form the student model
 * result(name,course,grade,profile, percentage, email status, batch)
 * 
 * placement 
 * take basic information form the student model 
 * placement (name, comany, sector gov/pvt , profile , package,)
 */
